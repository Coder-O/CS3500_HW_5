package cs3500.threetrios.controller;

/**
 * A testing class for {@link CompleteStrategyAdapter}.
 */
public class TestCompleteStrategyAdapter extends AbstractStrategyTest {
  // todo: Think of what to test

  private CompleteStrategyAdapter strategy;

  @Override
  public void reset() {
    super.reset();
    // todo: make a reset method that allows for differeing adaptions
    // strategy = new CompleteStrategyAdapter();
  }
}
